<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etycx.marry.modules.Recruitment.dao.RecruitmentDao">



	<select id="selectRecruitment" resultType="Recruitment">
		select id,work_type workType,name,num,responsibilities,email,address,create_time creatTime from marry_recruitment where status=#{status} order by create_time DESC limit #{page},#{pageSize}
	</select>

	<select id="selectRecruitmentCount" resultType="java.lang.Integer">
		select count(1) from marry_recruitment where status=#{status}
	</select>

</mapper>