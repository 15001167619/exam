<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etycx.marry.modules.notice.dao.NoticeDao">



	<select id="selectNotice" resultType="Notice">
		select id,title,Introduction introduction,content,author,create_time creatTime from marry_notice order by create_time DESC limit #{page},#{pageSize}
	</select>

	<select id="selectNoticeCount" resultType="java.lang.Integer">
		select count(1) from marry_notice
	</select>

	<select id="selectNoticeById" resultType="Notice">
		select id,title,Introduction introduction,content,author,create_time creatTime from marry_notice where id=#{id}
	</select>

</mapper>