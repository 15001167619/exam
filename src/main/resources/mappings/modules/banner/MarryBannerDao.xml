<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etycx.marry.modules.banner.dao.MarryBannerDao">
    
	<sql id="marryBannerColumns">
		a.id AS "id",
		a.name AS "name",
		a.cover_path AS "coverPath",
		a.sort AS "sort",
		a.create_time AS "createTime",
		a.update_time AS "updateTime",
		a.create_user_id AS "createUserId",
		a.update_user_id AS "updateUserId"
	</sql>
	
	<sql id="marryBannerJoins">
	</sql>
    
	<select id="get" resultType="MarryBanner">
		SELECT 
			<include refid="marryBannerColumns"/>
		FROM marry_banner a
		<include refid="marryBannerJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="MarryBanner">
		SELECT 
			<include refid="marryBannerColumns"/>
		FROM marry_banner a
		<include refid="marryBannerJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.sort DESC,create_time DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="MarryBanner">
		SELECT 
			<include refid="marryBannerColumns"/>
		FROM marry_banner a
		<include refid="marryBannerJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO marry_banner(
			name,
			cover_path,
			sort,
			create_time,
			update_time,
			create_user_id,
			update_user_id
		) VALUES (
			#{name},
			#{coverPath},
			#{sort},
			#{createTime},
			#{updateTime},
			#{createUserId},
			#{updateUserId}
		)
	</insert>
	
	<update id="update">
		UPDATE marry_banner SET 	
			name = #{name},
			cover_path = #{coverPath},
			sort = #{sort},
			create_time = #{createTime},
			update_time = #{updateTime},
			create_user_id = #{createUserId},
			update_user_id = #{updateUserId}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM marry_banner
		WHERE id = #{id}
	</update>
	
</mapper>